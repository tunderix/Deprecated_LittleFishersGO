using UnityEngine;
using System.Collections;

public class Bait : Item {
	public Bait(string name, string description, int value)
	{
		itemName = name;
		itemDesc = description;
		itemValue = value;
	}
}
